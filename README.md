# SFM-mobile

Agent Management System and Cron Job Service.

## Description
This project manages agents, products, and automated tasks using Node.js and Prisma.

## Getting Started
1. Install dependencies: `npm install`
2. Set up environment variables in `.env`
3. Run migrations: `npx prisma migrate dev`
4. Start the server: `npm start`
